<UserControl x:Class="GitHub.VisualStudio.UI.Views.HomeSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vsfx="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:ui="https://github.com/github/VisualStudio"
             Width="Auto"
             Height="auto"
             DataContext="{Binding ViewModel}"
             d:DesignHeight="68"
             d:DesignWidth="300"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="Hyperlink" BasedOn="{StaticResource {x:Static vsfx:VsResourceKeys.ThemedDialogHyperlinkStyleKey}}" />
    </UserControl.Resources>

    <StackPanel>
        <ui:OcticonImage
            Width="32"
            Height="32"
            Margin="0,0,8,0"
            VerticalAlignment="Center"
            DockPanel.Dock="Left"
            Icon="{Binding Icon}" />

        <Label Content="{Binding RepoName}" />

        <TextBlock TextAlignment="Left">
            <Hyperlink>
                <Run Text="{Binding RepoUrl}" />
            </Hyperlink>
        </TextBlock>
    </StackPanel>
</UserControl>
    