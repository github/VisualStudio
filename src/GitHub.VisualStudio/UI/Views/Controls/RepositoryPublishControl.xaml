<UserControl x:Class="GitHub.VisualStudio.UI.Views.Controls.RepositoryPublishControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:ui="clr-namespace:GitHub.UI;assembly=GitHub.UI"
             xmlns:uirx="clr-namespace:GitHub.UI;assembly=GitHub.UI.Reactive"
             xmlns:local="clr-namespace:GitHub.VisualStudio.UI.Views.Controls"
             xmlns:helpers="clr-namespace:GitHub.Helpers;assembly=GitHub.UI"
             xmlns:sampleData="clr-namespace:GitHub.SampleData;assembly=GitHub.App"
             xmlns:GitHub="clr-namespace:GitHub.VisualStudio.Helpers"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.14.0"
             xmlns:tfcrd="clr-namespace:Microsoft.TeamFoundation.Controls.WPF;assembly=Microsoft.TeamFoundation.Controls"
             xmlns:tl="clr-namespace:Microsoft.TeamFoundation.Controls.WPF.TeamExplorer;assembly=Microsoft.TeamFoundation.Controls"
             xmlns:ttvc="clr-namespace:Microsoft.TeamFoundation.Controls.WPF.Converters;assembly=Microsoft.TeamFoundation.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="432"
             d:DesignWidth="414"
             Margin="0"
             Background="Transparent">

    <d:DesignProperties.DataContext>
        <Binding>
            <Binding.Source>
                <sampleData:RepositoryPublishViewModelDesigner />
            </Binding.Source>
        </Binding>
    </d:DesignProperties.DataContext>

    <UserControl.Resources>
        <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource {x:Static vsshell:VsResourceKeys.ComboBoxStyleKey}}">
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="Height" Value="23" />
        </Style>
        <Style TargetType="{x:Type tfcrd:LabeledTextBox}">
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
    </UserControl.Resources>

    <StackPanel Margin="8,12,18,0" Orientation="Vertical" Style="{DynamicResource DialogContainerStackPanel}">
        <StackPanel.Resources>
            <ResourceDictionary>
                <ResourceDictionary.MergedDictionaries>
                    <tfcrd:TFCachedResourceDictionary
                        Source="pack://application:,,,/Microsoft.TeamFoundation.Controls;component/WPF/SharedResources.xaml" />
                </ResourceDictionary.MergedDictionaries>
            </ResourceDictionary>
        </StackPanel.Resources>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0">
            <ui:OcticonImage
                x:Name="octokit"
                Height="32"
                Width="32"
                VerticalAlignment="Center"
                Margin="0,6,8,8"
                Icon="mark_github"
                Foreground="{DynamicResource {x:Static vsshell:VsBrushes.ToolWindowTextKey}}" />
            <StackPanel Orientation="Vertical">
                <Label Content="GitHub" Foreground="{DynamicResource {x:Static vsshell:VsBrushes.CaptionTextKey }}" />
                <Label Content="GitHub, Inc." Foreground="{DynamicResource {x:Static vsshell:VsBrushes.GrayTextKey }}" Margin="0,-10,0,0" />
            </StackPanel>
        </StackPanel>

        <ComboBox x:Name="hostsComboBox">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Title}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <ComboBox x:Name="accountsComboBox">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image
                            Source="{Binding Avatar}"
                            Width="15"
                            Height="15"
                            RenderOptions.BitmapScalingMode="HighQuality"
                            Margin="0,0,8,0" />
                        <TextBlock Text="{Binding Login}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
            <ComboBox.ItemsSource>
                <x:Array Type="sampleData:AccountDesigner">
                    <sampleData:AccountDesigner />
                    <sampleData:AccountDesigner />
                    <sampleData:AccountDesigner />
                </x:Array>
            </ComboBox.ItemsSource>
        </ComboBox>

        <tfcrd:LabeledTextBox 
            x:Name="nameText"
            HintText="Repository Name"
            MaxLength="{x:Static GitHub:Constants.MaxRepositoryNameLength}"/>

        <StackPanel>
            <uirx:ValidationMessage
                x:Name="nameValidationMessage"
                ValidatesControl="{Binding ElementName=nameText}" />
            <uirx:ValidationMessage
                x:Name="safeRepositoryNameWarning"
                ValidatesControl="{Binding ElementName=nameText}"
                Icon="alert"
                Fill="#f39c12"
                Padding="2,0" />
        </StackPanel>

        <tfcrd:LabeledTextBox
            x:Name="description"
            Height="52"
            HintText="Description (optional)"
            AcceptsReturn="True" />

        <CheckBox x:Name="makePrivate" Margin="0,3,0,6" Background="{StaticResource ToolWindowBackgroundBrushKey}" BorderBrush="{StaticResource SubduedBorderBrushKey}">
            <TextBlock TextWrapping="Wrap">
                <TextBlock.Inlines>
                    <Run>Private Repository</Run>
                </TextBlock.Inlines>
            </TextBlock>
        </CheckBox>
        
        <Label x:Name="upgradeToMicroPlanWarning" HorizontalAlignment="Left" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                <TextBlock>Private repositories require a</TextBlock>
                <ui:OcticonLinkButton
                    x:Name="upgradeToMicroLink"
                    FontSize="12"
                    Icon="link_external" 
                    Content="micro plan"
                    ToolTip="upgrade"
                    Margin="5,0"/>
                <TextBlock>for $7/month</TextBlock>
            </StackPanel>
        </Label>
        <Label x:Name="upgradePlanWarning"  HorizontalAlignment="Left" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                <TextBlock>You're using 
                        <Run x:Name="ownedPrivateReposText"/>/<Run  x:Name="privateReposInPlanText"/>
                        private repositories.
                </TextBlock>
                <ui:OcticonLinkButton 
                    x:Name="upgradeAccountLink"
                    FontSize="12"
                    Icon="link_external" 
                    Content="Manage plans"
                    ToolTip="manage plans"
                    Margin="5,0"/>
            </StackPanel>
        </Label>

        <uirx:UserErrorMessages x:Name="userErrorMessages" />
        
        <Button
            IsDefault="True"
            x:Name="publishRepositoryButton"
            HorizontalAlignment="Left">
            <TextBlock>Publish</TextBlock>
        </Button>

        <tfcrd:BusySpinner x:Name="publishingSpinner" />

    </StackPanel>
</UserControl>
