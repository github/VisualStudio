<local:GenericPullRequestCreationView x:Class="GitHub.VisualStudio.UI.Views.PullRequestCreationView"
                                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                      xmlns:cache="clr-namespace:GitHub.VisualStudio.Helpers"
                                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                                      xmlns:helpers="clr-namespace:GitHub.VisualStudio.Helpers"
                                      xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                                      xmlns:local="clr-namespace:GitHub.VisualStudio.UI.Views"
                                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                                      xmlns:sampleData="clr-namespace:GitHub.SampleData;assembly=GitHub.App"
                                      xmlns:ui="clr-namespace:GitHub.UI;assembly=GitHub.UI"
                                  Background="{DynamicResource GitHubVsToolWindowBackground}"
                                  DataContext="{Binding ViewModel}"
                                      d:DesignHeight="450"
                                      d:DesignWidth="300"
                                      mc:Ignorable="d">

    <d:DesignProperties.DataContext>
        <Binding>
            <Binding.Source>
                <sampleData:PullRequestCreationViewModelDesigner />
            </Binding.Source>
        </Binding>
    </d:DesignProperties.DataContext>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <helpers:SharedDictionaryManager Source="pack://application:,,,/GitHub.VisualStudio;component/SharedDictionary.xaml" />
                <helpers:SharedDictionaryManager Source="pack://application:,,,/GitHub.UI;component/SharedDictionary.xaml" />
                <cache:SharedDictionaryManager Source="pack://application:,,,/GitHub.VisualStudio;component/Styles/GitHubActionLink.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <StackPanel Orientation="Vertical">
        <StackPanel Margin="12,0" Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center">From</TextBlock>
            <Label Content="fix-everything" />
            <TextBlock VerticalAlignment="Center">into</TextBlock>
            <ui:GitHubActionLink Content="master" HasDropDown="True" Margin="5,0,0,0" VerticalAlignment="Center">
                <i:Interaction.Behaviors>
                    <ui:DropDownActionLinkBehaviour />
                </i:Interaction.Behaviors>
                <ui:GitHubActionLink.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="master" />
                        <MenuItem Header="not-master" />
                    </ContextMenu>
                </ui:GitHubActionLink.ContextMenu>
            </ui:GitHubActionLink>
        </StackPanel>

        <ui:PromptTextBox PromptText="Title" />
        <ui:PromptTextBox Margin="12,5" PromptText="Description" TextWrapping="Wrap" AcceptsReturn="True" Height="100" />
        <Grid Margin="12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Assignee" VerticalAlignment="Center" />
            <ui:GitHubActionLink Grid.Column="1" Content="Haacked" HasDropDown="True" VerticalAlignment="Center" HorizontalAlignment="Left">
                <i:Interaction.Behaviors>
                    <ui:DropDownActionLinkBehaviour />
                </i:Interaction.Behaviors>

                <ui:GitHubActionLink.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Haacked" />
                        <MenuItem Header="not-Haacked" />
                    </ContextMenu>
                </ui:GitHubActionLink.ContextMenu>
            </ui:GitHubActionLink>

            <ui:GitHubActionLink Grid.Column="2" Content="Clear Assignee" VerticalAlignment="Center" />
        </Grid>

        <Grid Margin="12,6,12,12" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button HorizontalAlignment="Right" Content="Cancel" Grid.Column="0"/>
            <Button HorizontalAlignment="Right" Margin="6,0,0,0" Content="Create pull request" Grid.Column="1" Style="{DynamicResource GitHubPrimaryActionButton}" />
        </Grid>

        <TabControl>
            <TabItem Header="Commits">
                <StackPanel Orientation="Vertical">
                    <Grid Margin="10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Image VerticalAlignment="Top" Grid.Column="0" Grid.RowSpan="2" Source="/GitHub.VisualStudio;component/Resources/default_user_avatar.png"/>
                        <TextBlock Margin="5,0,0,0" Text="I am an example commit message" FontSize="14" Grid.Column="1" Grid.Row="0" />
                        <TextBlock Margin="5,0,0,0" Text="Andreia Gaita committed 20 hours ago" FontSize="11" Grid.Column="1" Grid.Row="1" />
                    </Grid>
                </StackPanel>
            </TabItem>
            <TabItem Header="Files Changed">
                <StackPanel Orientation="Vertical">
                    <Grid Margin="10,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>

                        <ui:OcticonImage
                            Grid.Column="0"
                            Height="16"
                            Width="16"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Icon="file_text" />

                        <Label Grid.Column="1" Content="src\start.cs" />
                        <StackPanel Orientation="Horizontal" Grid.Column="2">
                            <TextBlock VerticalAlignment="Center" Foreground="#ffbd2c00" Text="-14" />
                            <TextBlock Margin="5,0" VerticalAlignment="Center" Foreground="#ff6cc644" Text="+14" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Column="3">
                            <Border
                                Height="6" Width="6" 
                                BorderThickness="1"
                                Background="#FF6CC644" />
                            <Border
                                Height="6" Width="6" 
                                BorderThickness="1"
                                Background="#FF6CC644" />
                            <Border
                                Height="6" Width="6" 
                                BorderThickness="1"
                                Background="#FFBD2C00" />
                            <Border
                                Height="6" Width="6" 
                                BorderThickness="1"
                                Background="#FFBD2C00" />
                            <Border
                                Height="6" Width="6" 
                                BorderThickness="1"
                                Background="#FFCCCCCC" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </TabItem>
        </TabControl>
    </StackPanel>
</local:GenericPullRequestCreationView>
