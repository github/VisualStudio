<UserControl x:Name="userControl" x:Class="GitHub.VisualStudio.Views.ExpandoLabelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GitHub.VisualStudio.Views"
             xmlns:sampleData="clr-namespace:GitHub.SampleData;assembly=GitHub.App"
             xmlns:ui="clr-namespace:GitHub.UI;assembly=GitHub.UI"
             mc:Ignorable="d"
             Height="4" Width="16" ClipToBounds="False">

    <d:DesignData.DataContext>
        <sampleData:LabelViewModelDesigner Name="bug" BackgroundBrush="Red" ForegroundBrush="Black"/>
    </d:DesignData.DataContext>

    <Canvas Name="canvas">
        <Rectangle Name="rect"
                   Fill="{Binding BackgroundBrush}"
                   RadiusX="2"
                   RadiusY="2"
                   Width="{Binding ActualWidth, ElementName=canvas}"
                   Height="{Binding ActualHeight, ElementName=canvas}"/>
        <local:LabelView Canvas.Bottom="0"
                         IsHitTestVisible="False"
                         Visibility="{Binding IsExpanded, ElementName=userControl, Converter={ui:BooleanToVisibilityConverter}}"/>
    </Canvas>
</UserControl>
