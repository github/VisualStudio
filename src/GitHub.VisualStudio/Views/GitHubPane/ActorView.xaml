<UserControl x:Class="GitHub.VisualStudio.Views.GitHubPane.ActorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GitHub.VisualStudio.Views.GitHubPane"
             xmlns:sampleData="clr-namespace:GitHub.SampleData;assembly=GitHub.App"
             mc:Ignorable="d" d:DesignHeight="30" d:DesignWidth="30">

    <d:DesignData.DataContext>
        <sampleData:ActorViewModelDesigner/>
    </d:DesignData.DataContext>
    
    <Grid>
        <Border Name="avatarMask" Background="White" CornerRadius="3" />
        <StackPanel>
            <StackPanel.OpacityMask>
                <VisualBrush Visual="{Binding ElementName=avatarMask}" />
            </StackPanel.OpacityMask>
            <Image Source="{Binding Avatar}"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="HighQuality"/>
        </StackPanel>
    </Grid>
</UserControl>
