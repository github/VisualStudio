<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:GitHub.VisualStudio.Views.GitHubPane">

    <Style x:Key="BlackHyperlink" TargetType="{x:Type Hyperlink}">
        <Setter Property="Foreground" Value="{DynamicResource GitHubVsToolWindowText}" />
        <Setter Property="TextDecorations" Value="None" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="TextDecorations" Value="Underline" />
                <Setter Property="FrameworkElement.Cursor" Value="Hand" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground"  Value="{DynamicResource GHBlueLinkButtonDisabledTextBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="BlueHyperlink" TargetType="{x:Type Hyperlink}" BasedOn="{StaticResource BlackHyperlink}">
        <Setter Property="Foreground" Value="{DynamicResource GitHubActionLinkItemBrush}" />
    </Style>

    <Style TargetType="{x:Type Hyperlink}" BasedOn="{StaticResource BlackHyperlink}"/>

</ResourceDictionary>