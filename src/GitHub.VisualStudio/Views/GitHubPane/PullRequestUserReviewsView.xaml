<UserControl x:Class="GitHub.VisualStudio.Views.GitHubPane.PullRequestUserReviewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GitHub.VisualStudio.Views.GitHubPane"
             xmlns:c="clr-namespace:GitHub.VisualStudio.UI.Controls;assembly=GitHub.VisualStudio.UI"
             xmlns:cache="clr-namespace:GitHub.UI.Helpers;assembly=GitHub.UI"
             xmlns:ui="clr-namespace:GitHub.UI;assembly=GitHub.UI"
         mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <Control.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <cache:SharedDictionaryManager Source="pack://application:,,,/GitHub.VisualStudio.UI;component/SharedDictionary.xaml" />
                <cache:SharedDictionaryManager Source="pack://application:,,,/GitHub.UI;component/SharedDictionary.xaml" />
                <cache:SharedDictionaryManager Source="pack://application:,,,/GitHub.UI.Reactive;component/SharedDictionary.xaml" />
                <cache:SharedDictionaryManager Source="pack://application:,,,/GitHub.UI;component/Assets/Markdown.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <SolidColorBrush x:Key="GitHubExpanderHeaderBackground" Color="Transparent"/>
        </ResourceDictionary>
    </Control.Resources>

    <ScrollViewer>
        <StackPanel>
            <StackPanel DockPanel.Dock="Top" Margin="0 4" Orientation="Horizontal">
                <TextBlock FontSize="16" Margin="2 0 2 1" VerticalAlignment="Center">
                        <Run>Reviews by</Run>
                        <Run FontWeight="Bold" Text="{Binding User.Login, Mode=OneWay}"/>
                        <Run>for</Run>
                        <Hyperlink Command="{Binding NavigateToPullRequest}">
                            <Run>#</Run><Run Text="{Binding PullRequestNumber, Mode=OneWay}"/>
                        </Hyperlink>
                </TextBlock>
            </StackPanel>
            <TextBlock Foreground="{DynamicResource GitHubVsGrayText}" Text="{Binding Title}" TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal">
                <c:AccountAvatar Account="{Binding User}" Margin="4" Width="16" Height="16"/>
                <TextBlock Text="{Binding Reviews.Count, StringFormat={}{0} reviews}"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <ItemsControl ItemsSource="{Binding Reviews}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Rectangle Fill="{DynamicResource GitHubHeaderSeparatorBrush}" Height="1" Margin="0 4"/>

                            <Expander Style="{DynamicResource GitHubExpander}" IsExpanded="True">
                                <Expander.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <ui:OcticonImage Icon="check" Foreground="#2cbe4e" Visibility="{Binding State, Converter={ui:EqualsToVisibilityConverter Approved}}"/>
                                        <ui:OcticonImage Icon="x" Foreground="#cb2431" Visibility="{Binding State, Converter={ui:EqualsToVisibilityConverter ChangesRequested}}"/>
                                        <ui:OcticonImage Icon="comment" Visibility="{Binding State, Converter={ui:EqualsToVisibilityConverter Commented}}"/>
                                        <TextBlock FontWeight="Bold" Margin="2 0">
                                        <Run Text="{Binding Model.User.Login, Mode=OneWay}"/>
                                        <Run Text="{Binding State, Mode=OneWay}"/>
                                        <Run FontWeight="Normal">5 days ago</Run>
                                        </TextBlock>
                                    </StackPanel>
                                </Expander.Header>
                                <StackPanel Margin="16 4">
                                    <ui:SectionControl HeaderText="Description" IsExpanded="True">
                                        <TextBlock Text="{Binding Body}" Margin="0 4"/>
                                    </ui:SectionControl>
                                    <ui:SectionControl HeaderText="Comments" IsExpanded="True">
                                        <local:PullRequestFileCommentsView DataContext="{Binding FileComments}" Margin="0 4"/>
                                    </ui:SectionControl>
                                </StackPanel>
                            </Expander>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
