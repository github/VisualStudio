<UserControl x:Name="userControl" x:Class="GitHub.VisualStudio.Views.LabelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sampleData="clr-namespace:GitHub.SampleData;assembly=GitHub.App"
             mc:Ignorable="d">

    <d:DesignData.DataContext>
        <sampleData:LabelViewModelDesigner Name="bug" BackgroundBrush="Red" ForegroundBrush="Black"/>
    </d:DesignData.DataContext>

    <Border Name="border"
            Background="{Binding BackgroundBrush}"
            CornerRadius="2"
            Padding="2 0">
        <TextBlock Name="textBlock" Foreground="{Binding ForegroundBrush}" Text="{Binding Name}"/>
    </Border>
</UserControl>
